Compile with `pdflatex main.tex` (run twice for cross-references and the table of citations); no BibTeX needed — the bibliography is inline.
Figures are loaded from `../docs/paper-figures/` (fig1–fig4 `.pdf`); copy them next to `main.tex` and drop the `../docs/paper-figures/` prefix before uploading to arXiv.
