{
  "id"    : "{{ site.url }}{{ post.url }}",
  "title"   : "{{ post.title }}",
  "date"   : "{{ post.date | date_to_string }}",
  "description" : "{{ post.description | strip_html | strip_newlines | remove:'\"'  }}",
  "content" : "{{ post.content | strip_html | strip_newlines | remove:'\"'  }}"
}